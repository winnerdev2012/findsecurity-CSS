import {Collection} from "../../Types/CollectionTypes";

export const getGiveawayContent = (collection?: Collection | null,
                                   startDate?: Date,
                                   endDate?: Date | null): string => {
  const data = ['']
  return data.join(`\n`)
}
